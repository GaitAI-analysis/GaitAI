node_modules
.next
.next-*
# A build directory that got renamed out of the way of `.next` once, escaped
# the rule above, and was committed with 323 MB of webpack cache in it.
.next-acl-blocked
# Compiled output of the Cloud Function this project no longer has. The
# TypeScript source is gone (see firebase.json: Ask GaitAI's retrieval and
# generation both run in the browser), but `functions/lib/*.js` survived the
# deletion and reappears as untracked noise in every `git status`.
functions/lib
out
build
dist
# Secrets / environment — ignore every .env variant EXCEPT the committed template
.env
.env.*
!.env.example
# Credential / key files (never commit these)
*.pem
*.key
*.p12
*.pfx
*serviceAccount*.json
*service-account*.json
*-credentials.json
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel
public/uploads/*
!public/uploads/.gitkeep
build_check.log
devlog*.txt
fbtest.mjs
graphify-out/
# Screenshot output and the `--user-data-dir` profiles the headless-Edge
# verification workflow leaves behind: 31 MB of browser state in 1078 files
# the first time it went unignored.
tmp/

# Generated at dev/build time from .env.local — never commit
public/firebase-config.js
