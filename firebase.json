{
  "//": "Firestore rules only. There are no Cloud Functions: Ask GaitAI's retrieval and generation both run in the visitor's browser, so the one function this project had is gone. Hosting is deliberately not configured either — the site is a static export published to GitHub Pages, and a hosting target would invite `firebase deploy` to publish it from a developer machine.",
  "firestore": {
    "rules": "firestore.rules"
  }
}
