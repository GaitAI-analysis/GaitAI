{
  "name": "gaitai-functions",
  "description": "Ask GaitAI — the server-side half of the movement-intelligence guide.",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "test:retrieval": "tsx src/test-questions.ts",
    "test:answers": "tsx src/test-questions.ts --answers"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "typescript": "5.9.3",
    "tsx": "^4.23.13"
  }
}
